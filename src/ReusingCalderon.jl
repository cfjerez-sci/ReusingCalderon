"""
    ReusingCalderon

Project environment for the experiments accompanying

    P. Escapil-Inchauspe and C. Jerez-Hanckes,
    "Freezing Calderon: Robust Preconditioning for Maxwell Scattering under
    Shape Uncertainty", submitted to SIAM J. Sci. Comput. (2026).

The module itself is intentionally empty. It exists so that `Project.toml`
names a package and the pinned `Manifest.toml` resolves unchanged; every
experiment lives in `scripts/`, with the shared pieces in `methods/`,
`problems/`, `postproc/` and `almond/`.
"""
module ReusingCalderon
end
